<nav class="py-3 shadow">
  <div class="flex items-center justify-between m-auto flex-wrap container m-auto px-6">
    <div class="flex items-center flex-shrink-0 text-white mr-6">
      <%= link_to "ORIGIN", root_path, class: "font-silom text-navy font-black text-xl tracking-wider" %>
    </div>
    <div class="block lg:hidden">
      <button
        class="flex items-center px-3 py-2 border rounded border-gray-800"
        id="toogle-menu"
      >
        <svg
          class="fill-current h-3 w-3"
          viewBox="0 0 20 20"
          xmlns="http://www.w3.org/2000/svg"
        >
          <title>Menu</title>
          <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z" />
        </svg>
      </button>
    </div>
    <div
      class="hidden w-full block flex-grow lg:flex lg:items-center lg:justify-end lg:w-auto"
      id="menu"
    >
      <div class="text-sm text-black">
        <% if signed_in? %>
          <%= render 'layouts/shared/menu_options' %>
        <% else %>
          <%= link_to(
            "Login",
            log_in_path,
            class: "block mt-4 lg:inline-block lg:mt-0 mr-4 hover:text-blue-500"
          ) %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
